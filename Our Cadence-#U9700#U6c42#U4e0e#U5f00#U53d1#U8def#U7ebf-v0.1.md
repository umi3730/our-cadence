# Our Cadence：需求与开发路线 v0.1

日期：2026-09-16  
状态：立项建议与技术核查，未开发、未进行音乐效果或性能验收。

## 1. 已确认的目标

- 当前只整理需求、可行性和开发顺序，不开始应用开发。
- 第一阶段面向自己和少量朋友，验证 OC 主题曲体验。
- 产品方向来自用户提供的《思考了 50s.docx》：图片辅助建立角色音乐主题，同一角色在不同场景中保持主题身份，并允许用户编辑。
- 附件是需求参考。其模型能力、参数权重、效果和性能描述不作为已验证事实。

建议的产品定位：**为一个 OC 创建可保存、可编辑的主题旋律，再围绕它制作不同场景的纯音乐配乐。**

“图片转音乐”负责启动创作；“同一主题的持续创作”是本次优先验证的价值。尚未对这一产品方向进行完整竞品查重，不宣称独创。

## 2. 第一版用户流程

1. 创建角色档案，填写名字、可选的角色设定，上传一张图片；也可向已有角色添加新图。
2. 系统提出画面氛围与配器建议，用户可以纠正。系统不把图片中的颜色或姿态当成角色性格的确定事实。
3. 生成三段短主题候选，逐一试听；用户选择一个作为角色主题。
4. 基于选定主题生成“日常、回忆、战斗”三种编曲。
5. 在四轨工作区试听，修改旋律音符、换音色、调速度或轨道音量。
6. 保存版本，导出 MIDI 或 WAV。再次打开时读取保存的工程。
7. 下次给同一角色上传新场景图，沿用该角色已选主题，生成新的编曲版本。

新增图片默认不重写角色主题。修改主题要创建新的主题版本，已有作品继续引用旧版本。

## 3. 建议冻结的 MVP 范围

| 模块 | 第一版包含 | 后续再考虑 |
| --- | --- | --- |
| 输入 | PNG/JPEG/WebP 静态图、可选角色设定；建议限制 10 MB 并校验解码后像素数 | 动图、视频、多角色自动拆分 |
| 角色管理 | 手动创建、选择角色；一角色多图 | 自动识别和合并角色 |
| 图片理解 | 一次视觉模型分析，输出可修改的氛围、能量、场景与配器建议 | 主体分割、多个视觉模型融合、向量数据库 |
| 主题 | 三个 2–4 小节候选，选定后保存音符与节奏 | 自动评价“最符合角色”的主题、长篇主题生成 |
| 编曲 | 固定 4/4 拍、16 小节；旋律、和弦、贝斯、鼓四轨；三种场景编曲 | 任意结构、拍号、无限轨道、完整歌曲 |
| 编辑 | 旋律网格增加/拖动/删除音符、调整音长；选择预设和弦方案；轨道音色/音量/静音/独奏；全局 BPM；撤销/重做 | 所有轨道完整钢琴卷帘、音频剪辑、插件、自动混音 |
| 保存 | 自动保存草稿、明确保存版本、角色主题版本关联 | 多人实时协作、公开作品社区 |
| 导出 | 多轨 MIDI、采样音色渲染的 WAV | MP3、视频、AI 成曲及音频分轨 |
| 小范围试用 | 邀请制访问、各用户项目隔离；由作者导出文件供朋友试听 | 公开注册、支付、排行榜、公开分享链接 |
| 找 BGM | 不进入本轮 MVP | 建立可播放且允许使用的曲库后，独立验证推荐功能 |

16 小节的秒数随 BPM 改变。例如 4/4 拍、120 BPM 时是 32 秒，不同时承诺“固定小节数”和“严格 30 秒”。

第一版固定主题的音高与节奏；场景变化主要作用于配器、鼓型、伴奏密度、力度和整体速度。移调、主题节奏改写等变奏等到能稳定识别主题后再加入。

## 4. 附件中需要修正的技术判断

| 原设想 | 更可靠的处理 |
| --- | --- |
| 相同图片生成结果 100% 一样 | 已保存的同一工程可直接返回原结果。重新推理不承诺逐字节相同；必须保存实际音符和音频，不能只保存 seed。 |
| SHA256、pHash 可以统一认定同图 | SHA256 用于精确文件匹配；必要时另存规范化像素摘要。pHash 只作为近似候选，不能直接合并角色或工程。 |
| 相同角色的 embedding 必然接近 | 不保证跨画风、服装和构图仍然可靠。第一版以用户选择的 Character ID 为准；未来相似度只给归档建议。 |
| 红色、夜景等可以推出确定的情绪、调性 | 映射属于创作建议。允许用户修改，保存用户选定版本；不显示未经校准的“匹配度 94%”。 |
| 主体 50%、语义 20% 等固定权重 | 暂无本项目验证依据，第一版不使用这组数值作为算法承诺。 |
| 高张力直接对应某组和弦 | 和弦的感受还受节奏、音色、音区和上下文影响；用经过试听的编曲模板验证。 |
| MIDI 可以直接送给 ACE-Step 高质量渲染 | 先用采样器把 MIDI 渲染为音频，再以源音频做 Cover 等实验。输出是重新生成的音频，不保证每个音符严格对应原 MIDI。 |
| AI 成曲能继续像 MIDI 一样逐音符编辑 | 编辑工程保留 MIDI；AI 成曲另存为音频衍生版本。不能把音频分轨等同于恢复准确 MIDI。 |

SigLIP 官方文档描述的是图像/文本表征与相似度能力，不是面向 OC 的跨图片身份保证。因此手动归档是当前的产品决策，而非模型已解决身份问题。[SigLIP 文档](https://huggingface.co/docs/transformers/model_doc/siglip)

ACE-Step 官方 Cover API 使用 `src_audio` 与风格描述控制音频改编；Lego、Extract 等能力还依赖模型类型。它适合后续成曲实验，不能代替确定性的 MIDI 音源。[ACE-Step 推理 API](https://ace-step.github.io/ACE-Step-1.5/en/INFERENCE)

## 5. 技术路线

### 5.1 两条生成路线分阶段建设

第一版：

```text
角色档案 + 图片 + 用户设定
              ↓
视觉分析 → 用户可修正的音乐设定
              ↓
主题候选 → 用户选定 → 保存角色主题版本
              ↓
受控四轨编曲器（日常 / 回忆 / 战斗）
              ↓
音符工程 → 网页编辑 → 采样音色试听 → MIDI / WAV
```

后续实验：

```text
已保存的音符工程 → 采样器渲染音频 → ACE-Step 音频改编
                                              ↓
                                 新音频版本 + 来源工程版本
```

编辑原工程后，已经生成的 AI 音频不会自动变化；需要明确重新生成，并保留旧版本。

### 5.2 模块选择

| 模块 | 建议 | 选择原因 |
| --- | --- | --- |
| 网页 | React + TypeScript，单个创作工作区 | 第一版操作集中，不需要三个完整子产品 |
| 音频 | Tone.js / Web Audio，少量固定采样音色 | 让音符工程可直接试听，先验证配乐效果 |
| API 与作曲 | Python + FastAPI | 文件处理、音乐事件生成、视觉接口集中处理 |
| 初期持久化 | 单实例 SQLite + 私有文件目录，定期备份 | 少量试用者不需要先引入向量数据库或多个服务 |
| 图片理解 | 接入一种可用的视觉模型，通过服务端调用 | 先验证图片和用户设定是否改善结果，再决定是否本地部署 |
| 主题与编曲 | 规则约束的主题候选生成 + 人工打磨的和弦、贝斯和鼓模板 | 便于保持主题、解释改动、控制失误 |
| AI 音符模型 | MIDI-GPT 作为第二阶段对照实验 | 只有试听证明优于规则基线时才集成 |
| AI 音频 | ACE-Step 单独实验与部署 | 与编辑、存档、试听主流程解耦 |

Tone.js 支持浏览器交互音乐及离线音频渲染；导出 WAV 还需要采样加载完成与文件编码流程，不是只有一个播放按钮。[Tone.js 项目](https://github.com/Tonejs/Tone.js)、[Offline 渲染文档](https://tonejs.github.io/docs/14.3.40/fn/Offline)

MIDI-GPT 官方提供多轨生成、缺失小节填充和音符密度等属性控制；这些能力不自动保证主题好听或符合某个 OC，必须与规则基线盲听比较。[MIDI-GPT 官方项目](https://github.com/Metacreation-Lab/MIDI-GPT)

### 5.3 “音乐设定”数据应拆开

不要将所有内容混成一个不可解释的 DNA 向量。至少区分：

- 观察建议：图片色彩、场景及氛围描述，分析模型与版本。
- 用户意图：角色设定、用户纠正、希望的风格；优先级高于模型建议。
- 角色主题：主题音符、节奏、调性和主题版本，是作品保持身份的依据。
- 场景编曲：速度、伴奏模板、音色和轨道参数，可随每张图改变。

前台可称为“角色音乐设定”。不向普通用户暴露 embedding、哈希和未经验证的小数分数。

### 5.4 最小数据实体

| 实体 | 关键内容 |
| --- | --- |
| Character | 所有者、名字、角色设定、当前主题版本 |
| ImageAsset | 所有者、所属角色、文件摘要、私有位置、分析建议及分析版本 |
| ThemeRevision | 角色、音高/节奏、用户选择、来源、生成器版本与 seed |
| ProjectRevision | 主题版本、场景、BPM、四轨音符、音色版本、编辑记录或完整快照 |
| Render | 来源工程版本、渲染器/模型版本、参数、状态与音频位置 |

生成记录是不可变版本，草稿另存。相同图片在不同用户、角色、场景下不直接共享工程缓存；缓存必须包含所有者、角色主题版本、设置与算法版本。

### 5.5 小范围朋友试用的最低保障

- 邀请制登录；每个项目、图片和导出请求均校验所属用户，不能只在页面隐藏按钮。
- 图片默认私有，不自动发布到画廊。外部视觉分析前说明发送到哪里，提供手动填音乐设定的替代入口。
- 校验文件真实类型、大小与解码尺寸；模型密钥保存在服务端。
- 生成失败要显示真实状态并允许重试，不能展示预置音乐冒充该图片的生成结果。
- 保存任务状态；重试不重复覆盖工程；AI 长任务在后续通过独立任务执行器处理。
- 服务使用持久磁盘与备份；迁往多实例或更大规模后再迁移数据库和对象存储。

## 6. 开发顺序与退出条件

以下为一名熟悉 Web 开发的开发者的粗估，不是交付承诺。音乐编曲经验、现有模型服务及反馈速度会显著影响耗时。

| 阶段 | 工作 | 粗估 | 通过后才继续 |
| --- | --- | --- | --- |
| A：声音可行性样机 | 先用手工填写的音乐设定，做 3–5 个角色主题及三种编曲；生成 MIDI/WAV 供试听 | 3–5 个工作日 | 至少两位试用者愿意保留某个主题；变奏之间能辨认关联；不满意则先调整音乐引擎 |
| B：图片与主题流程 | 接入一个视觉模型；图片上传、角色归档、主题候选、用户纠正、工程版本保存 | 4–6 个工作日 | 换图不覆盖主题；同一工程可恢复；错误有明确反馈 |
| C：四轨编辑器 | 播放与循环、旋律编辑、预设和弦、音色/音量/静音/独奏、撤销与导出 | 6–10 个工作日 | 改音符立即影响下一次播放；MIDI/WAV 与当前保存版本一致 |
| D：朋友试用 | 邀请与项目隔离、上传校验、部署备份、基础浏览器测试、体验修复 | 3–5 个工作日 | 用户能独立完成上传到导出；不同用户项目相互不可访问 |

合计约 16–26 个工作日；日历计划建议留 4–6 周。邀请与所有权字段从 B 阶段的数据设计开始考虑，D 阶段才向朋友开放。

先验证声音，不先做完整 UI；不要把“界面能点击”当成“OC 主题曲效果成立”。A 阶段若失败，暂停扩展，优先调整主题和编曲，或评估 MIDI-GPT 的增益。

后续优先级：先验证 AI 成曲是否保留主题，再考虑局部重写、更多场景与自动角色归档。已有歌曲推荐作为另一项独立功能立项。

## 7. 验收方案

下列均为待执行的验收目标，目前没有任何实测结果。

### 产品效果

- 准备 5 个 OC，每个 3 张不同场景图片；附上创作者认可的简短设定。
- 同时试听“仅手填设定”和“图片分析后可修改设定”两组，记录图片输入是否带来实际帮助。
- 每个角色选择主题后制作三种编曲，隐藏名字做小规模辨认，记录被误认为其他角色的组合。
- 邀请至少 3 位朋友，记录最愿意保留的主题、是否符合设定、变奏是否过于相似，以及愿不愿继续编辑。
- 不把少量试用结果外推为普遍准确率；若大多数人都不想保存任何候选，音乐质量尚未通过。

### 可自动验证的行为

- 锁定主题后，三种基础编曲保留相同的主题音符与相对拍点；BPM 可以不同。
- 打开已保存版本时，角色、主题、音符和参数完整恢复；不重新调用生成服务。
- 新图、新场景、新主题候选不会覆盖旧作品；撤销恢复到上一编辑状态。
- 导出的 MIDI 有正确的轨道、速度、音符时长与乐器信息；不同播放器的音色差异不误判为工程错误。
- WAV 与所选工程版本、采样音色一致；无丢轨、明显削波和异常中断。
- 不同用户无法凭图片、工程或导出 ID 读取对方资源。
- 模型超时、坏文件、刷新和重复点击有可恢复行为；失败不能显示“已生成”。

### 性能验证

编辑时实时试听，不每次修改都请求 AI。正式给出生成耗时前，记录设备/服务配置、图片大小、曲长、冷启动与热启动时间。当前没有可依赖的性能数字。

## 8. 成本、条件与尚未确定的事项

- A 阶段不依赖音乐生成 GPU；可用手填设定和固定音源完成声音样机。
- B 阶段需要选定视觉模型服务或本地模型。尚未选择供应商、提供密钥或产生调用费用。
- 朋友试用需要可访问的私有部署环境和持久存储。当前未选择主机、域名或认证服务，也未部署。
- 成本按“有效图片分析次数 × 单次调用成本 + 托管与存储”记录；接入 AI 成曲后增加 GPU/音频生成费用。服务选定前不报固定月费。
- 采样音色需要确认允许网页使用与分发；具体音源尚未选定。
- 不需要现在购买显卡。ACE-Step 不同模型配置要求差异很大：官方当前 XL 路线列出至少 12 GB 显存（配合 offload/量化），推荐 20 GB 及以上，不能把较小模型的宣传值套到所有配置。[ACE-Step 官方 README](https://github.com/ace-step/ACE-Step-1.5/blob/main/README.md)

下一步建议：执行 A 阶段“声音可行性样机”，先产出能听的角色主题与三种编曲；效果通过后，再进入完整网页开发。
