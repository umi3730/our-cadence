# 2026-09-18 六维参数、视觉与谱曲更新

本次在 `feat/studio-profile-ui` 分支、`98d66b5` 基础上整理当前会话的修改，执行本地 Git 提交，不推送或合并。

## 工作室

- 移除“攻击性”滑条、参数说明和该项参与的前三项性格展示，界面保留能量、温度、张力、神秘、明亮、优雅六项。
- 空状态文案同步为“六项参数”；主题卡片描述改成音乐气质用语，避免继续显示“高／低攻击性”。
- 为兼容旧 JSON 工程，内部 `aggression` 字段及既有生成逻辑保留。本次不是存档迁移，也不重写用户已保存的作品。
- BPM、调式和音色倾向收进默认折叠的“图片解读与参数说明”。
- “换一组灵感”使用 Shuffle 图标，改成蓝紫粉浅渐变、深紫文字和彩色细边，支持悬停、键盘焦点、按下和减少动态效果状态。

## 首页视觉

- 中文说明与眉题使用本地未来荧黑 Regular 子集，附 OFL 授权与来源记录；英文大标题仍使用原 Inter 斜体。
- 三页眉题统一中英文基线、字号和间距；中文分隔改为中点，英文用自然大小写。
- 大标题按 O／C、theme／scenes、Your own. 分别使用雾蓝、柔紫和玫瑰紫；入口按钮交互色与章节配套。
- 正文关键词加入淡色底纹，进场时播放一次轻扫动画，系统减少动态效果时静态显示。
- 左上角标语保留轻量紫灰字与渐变细线。流星、云朵加入播放与暂停控制，原有鼠标星光保留。
- Lordicon 素材、来源清单和 Lottie 播放器依赖随源码保存。首页署名按用户要求移除；素材库及来源说明仍保留授权记录，公开发布前的授权事项已注明。

## 谱曲与音频

- 一并提交尚未入库的 GPT-6 Astra 符号谱曲、独立公司网关配置、乐谱校验及工程保存功能，详情见 [9 月 17 日接入记录](2026-09-17_GPT6谱曲接入.md)。
- 提示词按“整体太乱”的反馈减少声部竞争、鼓点和大跳；长笛、小提琴、马林巴接入采样、合成备用音色、存档与 MIDI 导出。
- 修复首次总音量读取、主题试听混音隔离，采样下载增加超时与已有备用路径；公司请求禁止自动跟随重定向。
- 真实公司谱曲、音频渲染和各新音色试听在前次会话完成并留档；本次提交检查不再触发付费生成。

## 本次验证

- 浏览器确认参数列表为能量、温度、张力、神秘、明亮、优雅，攻击性滑条数量为 0。
- TypeScript 类型检查通过。
- 生产构建通过。
- 完整测试 **21 项：20 项通过，1 项失败**。失败为继承的规则主题音符超出声明调式问题（`tests/core.test.mjs` 的首项），此前已有记录，本次未跳过或放宽断言。
- 提交前检查暂存区文件、差异及敏感信息；本地 API Key、浏览器工程、试听音频、下载缓存、生成目录和依赖目录不纳入 Git。

## 范围与已知限制

- 首页与工作室源码、必要运行素材及相关文档一并提交；没有部署操作。
- Suno 仅做过接入调研，未接入。高级识图仍走原官方配置，公司网关目前仅用于谱曲。
- 字体网页子集覆盖当前首页文案；未来新增中文字符时需更新子集。
