# Our Cadence v0.5

- 当前会话图片预览直接使用上传原图 Object URL，不再用 176px 小缩略图放大；工程重载 fallback 提升为最长边 900px WebP。
- 新增可选高级语义识图 `/api/image-understand`：分开理解主体与背景，可识别已知游戏/动漫角色、meme/梗图；在需要时使用 Web Search 查找常见 BGM/梗曲关联。
- 新增 SUBJECT CONTENT / BACKGROUND CONTENT / meme 语境 / 常见关联配乐展示。
- “快速方向”和“联网音乐匹配”默认折叠。
- 联网找歌改为多阶段：Music Profile 目标 -> 多平台候选召回 -> 角色/梗关联加权 -> 对公开 preview 做实际音频特征分析后二次排序。
- Apple / Deezer preview 通过同源代理分析 RMS、动态、粗略 BPM、高低频比例、瞬态等；QQ音乐 / 网易云 / B站继续参与候选召回与实体关联。
- 三个 Theme 改为明显不同的抒情 / 推进 / 氛围三种原型，分别使用不同 BPM、节奏密度、音域、和声进行与旋律轮廓。
- 播放优先使用 FluidR3_GM SoundFont 真实采样：钢琴 / Celesta / 钢弦吉他 / 弦乐 / 指弹贝斯，CDN 不可用时自动回退原合成器。
- 新增 `配置高级识图.bat`。

## 高级识图配置

在 `studio` 目录双击 `配置高级识图.bat`，或手动新建 `.env.local`：

```env
OPENAI_API_KEY=your_key_here
OPENAI_VISION_MODEL=gpt-5.6-luna
```

`.env.local` 已被 `.gitignore` 忽略，不要提交密钥。

## SoundFont attribution

Sample playback uses the pre-rendered FluidR3_GM General MIDI soundfont from `gleitz/midi-js-soundfonts`, distributed under CC BY 3.0. The original project is https://github.com/gleitz/midi-js-soundfonts .
